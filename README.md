# D&D 5e Loot Generator

A standalone **offline loot generator** for *Dungeons & Dragons 5e*, using the SRD random treasure tables.  
Includes a **Tkinter GUI** for generating loot for individual enemies or hoards.

---

## 🧰 Features
- SRD-style individual and hoard treasure tables
- Magic item tables (A–H) included
- Optional **custom item JSON** loader
- Add extra items manually or by CR range
- Copy-to-clipboard support
- 100% offline (no API calls required)

---

## 🧩 Requirements

- **Python 3.10 or later**
- `tkinter` (included with most Python installs)

Install dependencies (if needed):

```bash
pip install -r requirements.txt
